var App = require("../../Application");

module.exports = async (authModel) => {
    try {
        let credential = App.Dependencies.SCI.Auth.Authenticate
    }
    catch (erro) {
        throw erro;
    }
}